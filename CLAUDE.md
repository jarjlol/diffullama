@AGENTS.md

## Claude Code only

- Personal, uncommitted overrides go in `CLAUDE.local.md` (gitignored).
- This repo has no lint/test command to run after edits — verify by running the specific
  script you changed and pasting its real output, not by asserting it works.
- Long research/verification passes: prefer reading the primary PDF directly over a
  WebFetch summary. Summaries of papers have been wrong or generic here more than once.
